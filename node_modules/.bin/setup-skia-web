#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@shopify/react-native-skia/scripts/setup-canvaskit.js" "$@"
  ret=$?
else 
  node  "$basedir/../@shopify/react-native-skia/scripts/setup-canvaskit.js" "$@"
  ret=$?
fi
exit $ret
